# Codex Project Instructions

## Communication

- 默认使用中文回复。
- 给出代码时，要给出详细解释：为什么要这么做，以及这样做改变了什么。
- 如果只是执行命令或修改文件，说明关键结果即可，避免堆太多无关输出。

## Workspace Scope

- 项目根目录是 `E:\datastr`。
- 可以在项目根目录内新建、编辑、移动、删除文件，以完成用户明确提出的任务。
- 不要读取、修改、删除项目根目录以外的任何文件，包括用户主目录、全局 Codex 配置、其他仓库、系统目录，除非用户明确要求并同意。
- 如果某个命令可能写入项目外路径，必须先说明原因并征求用户确认。

## File Safety

- 删除项目内文件是允许的，但要保持克制。
- 可以直接删除自己刚创建的临时文件、缓存文件或确认无用的生成文件。
- 删除以下内容前需要先确认：数据集、数据库、虚拟环境、密钥文件、配置文件、大批量文件、Git 历史相关文件。
- 不要修改 `.git` 目录内容，除非用户明确要求执行 Git 操作。
- 不要覆盖用户已有改动；如果发现工作区已有未提交修改，要把它们当作用户改动来保护。

## Git

- 不要自动 commit、push、reset、rebase、checkout 或切换分支，除非用户明确要求。
- 在较大范围修改前，先查看当前 Git 状态。
- 禁止使用 `git reset --hard`、`git checkout -- <file>` 等会丢失改动的命令，除非用户明确要求并确认风险。

## Project Data

- `data/` 目录包含项目数据和脚本，改动前要先理解用途。
- 不要随意删除或重建 `data/venv/`，除非用户要求处理 Python 环境。
- 大型 CSV 或采集结果默认视为重要数据，不要在没有确认的情况下重写或删除。

## Course Project Context

- 本项目是《数据结构大作业：短视频推荐系统》。
- 开始设计、编码或重构前，优先查看 `docs/assignment-requirements.md`，确认改动服务于 F1-F7 中的哪一项。
- 系统设计参考 `docs/system-design.md`，数据字段参考 `docs/data-schema.md`，算法选择参考 `docs/algorithm-design.md`。
- 每个核心功能都要能解释：输入是什么、输出是什么、用了什么数据结构、为什么这样做、复杂度如何。
- 优先选择适合课程答辩的可解释算法，不要为了炫技引入难以说明的复杂模型。

## Coding Preferences

- 优先沿用项目已有结构和命名风格。
- 新增脚本时，优先放在与用途最接近的目录中。
- 新增依赖前先说明为什么需要；能用标准库或现有依赖解决时，优先不加新依赖。
- 对数据处理代码，优先使用清晰的函数边界、可重复运行的流程、明确的输入输出路径。

## Verification

- 修改代码后，尽量运行最小必要验证，例如脚本语法检查、单元测试或一次小样本运行。
- 如果因为环境、依赖、时间或权限无法验证，要明确说明没有验证成功以及原因。
